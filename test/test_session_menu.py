import pytest
from kivy.clock import Clock

from appI2C.session.session_menu import SessionMenuRegular, SessionMenuEmptySession # type: ignore

#parametrize = pytest.mark.parametrize

class TestSessionMenuRegular:

    # TBD: Check if class sessionMenuRegular has all its attributes with hasattr()

    #def test_edit_session(self, capsys):

    #def test_go_back_to_scrollable_field(self):

    """ def test_start_session(self, *args):
        isinstance("?", Clock) """

    #def test_switch_to_next_view(self, *args):


    """ def test_this_is_for_testing_initially(self):
        with pytest.raises(SystemExit):
            SessionMenuRegular.this_is_for_testing_initially()
        #self.value=1
        #SessionMenuRegular.edit_session()
        #assert self.value == y
        #out,err=capsys.readouterr()
        #assert out=="Editing sessions will be implemented in the future" """

""" class TestSessionMenuEmptySession(FloatLayout):

    def test_go_back_to_scrollable_field(self):

    def test_switch_to_edit_session(self, *args): """
